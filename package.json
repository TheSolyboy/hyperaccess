{
  "name": "hyperaccess",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal MCP server exposing a single run_command tool over authenticated HTTP.",
  "license": "MIT",
  "type": "module",
  "main": "build/index.js",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^5.1.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
